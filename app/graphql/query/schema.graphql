type Query {
  # 权限列表，管理页面使用
  getPermissionList: Res
  # 权限状态列表，登录使用
  getPermissionListForLogin: Res
  # 权限状态列表，管理使用
  getPermissionStateList: Res

  # ================
  # todo

  getAllTodoNode: ResTodoNodeList

  # ResTodo ResTodoList TodoInput

  getTodoListByNode(node: String!): ResTodoList
  getTodoListByState(node: String!): ResTodoList
  getTodoById(id: ID!): ResTodo
}
